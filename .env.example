# OpenClaw Gateway Configuration
# Gateway token (required for server-side API calls)
# Retrieve from: openclaw gateway status
NEXT_PUBLIC_GATEWAY_URL=http://localhost:7070
GATEWAY_TOKEN=your_gateway_token_here

# Optional: Gateway hostname override
# GATEWAY_HOST=localhost
# GATEWAY_PORT=7070
